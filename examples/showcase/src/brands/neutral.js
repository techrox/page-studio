// Neutral brand — no block defaults override. The editor and renderer fall
// through to each block's intrinsic defaultProps from
// @techrox/page-studio-blocks. This is the "out of the box" experience.

export const neutralDefaults = undefined

export const neutralBranding = {
  id: 'neutral',
  name: 'Neutral',
  description: 'No overrides — the package defaults.',
  primaryColor: '#0F766E',
  accentColor: '#F59E0B',
  inkColor: '#0F172A',
}
